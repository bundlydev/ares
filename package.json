{
  "name": "@bundly/ic-connect",
  "version": "1.0.3",
  "description": "Monorepo for ic-connectors",
  "author": "Bundly",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules && rm -rf .turbo",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "prettier": "^3.2.4",
    "turbo": "^1.11.3"
  }
}
